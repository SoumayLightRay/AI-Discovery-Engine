services:
  - type: web
    name: discovery-engine-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/phase_03/server.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
    plan: free
